import CryptoListContainer from "@/components/CryptoListContainer";

const page = () => {
  return (
    <section>
      <CryptoListContainer type="receive" />
    </section>
  );
};

export default page;
